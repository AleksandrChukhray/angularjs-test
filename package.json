{
  "name": "angular-test",
  "private": true,
  "version": "0.0.1",
  "description": "A test project on angular js",
  "repository": "https://github.com/",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "start": "http-server ./app -a localhost -p 8000 -c-1"
  }
}
